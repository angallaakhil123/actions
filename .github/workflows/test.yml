name: work-2
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-22.04
   steps:
     - name: build
       run: echo "building"
  test:
   runs-on: ubuntu-22.04
   steps:
     - name: test
       run: echo "test"
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - name: deploy
        run: echo "deploy"
